# Bypass flags — see wiki/bypass-flags.md
# raw   = keep WPBakery shortcodes visible, keep original desktop nav behaviour
# fixed = strip shortcodes, apply desktop nav fix
REACT_APP_RENDER_MODE=fixed
